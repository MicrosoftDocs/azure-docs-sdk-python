### YamlMime:PythonClass
uid: azure.mgmt.rdbms.postgresql_flexibleservers.models.Network
name: Network
fullName: azure.mgmt.rdbms.postgresql_flexibleservers.models.Network
module: azure.mgmt.rdbms.postgresql_flexibleservers.models
inheritances:
- azure.mgmt.rdbms.postgresql_flexibleservers._serialization.Model
summary: Network properties of a server.
constructor:
  syntax: 'Network(*, public_network_access: str | _models.ServerPublicNetworkAccessState
    | None = None, delegated_subnet_resource_id: str | None = None, private_dns_zone_arm_resource_id:
    str | None = None, **kwargs: Any)'
  parameters:
  - name: public_network_access
    description: 'public network access is enabled or not. Known values are:

      "Enabled" and "Disabled".'
    types:
    - <xref:str>
    - <xref:azure.mgmt.rdbms.postgresql_flexibleservers.models.ServerPublicNetworkAccessState>
  - name: delegated_subnet_resource_id
    description: 'Delegated subnet arm resource id. This is required to be

      passed during create, in case we want the server to be VNET injected, i.e. Private
      access

      server. During update, pass this only if we want to update the value for Private
      DNS zone.'
    types:
    - <xref:str>
  - name: private_dns_zone_arm_resource_id
    description: 'Private dns zone arm resource id. This is required

      to be passed during create, in case we want the server to be VNET injected,
      i.e. Private access

      server. During update, pass this only if we want to update the value for Private
      DNS zone.'
    types:
    - <xref:str>
variables:
- description: 'public network access is enabled or not. Known values are:

    "Enabled" and "Disabled".'
  name: public_network_access
  types:
  - <xref:str>
  - <xref:azure.mgmt.rdbms.postgresql_flexibleservers.models.ServerPublicNetworkAccessState>
- description: 'Delegated subnet arm resource id. This is required to be

    passed during create, in case we want the server to be VNET injected, i.e. Private
    access

    server. During update, pass this only if we want to update the value for Private
    DNS zone.'
  name: delegated_subnet_resource_id
  types:
  - <xref:str>
- description: 'Private dns zone arm resource id. This is required to

    be passed during create, in case we want the server to be VNET injected, i.e.
    Private access

    server. During update, pass this only if we want to update the value for Private
    DNS zone.'
  name: private_dns_zone_arm_resource_id
  types:
  - <xref:str>
