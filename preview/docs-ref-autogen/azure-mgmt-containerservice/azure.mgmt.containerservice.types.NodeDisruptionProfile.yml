### YamlMime:PythonClass
uid: azure.mgmt.containerservice.types.NodeDisruptionProfile
name: NodeDisruptionProfile
fullName: azure.mgmt.containerservice.types.NodeDisruptionProfile
module: azure.mgmt.containerservice.types
summary: Node disruption profile for a managed cluster.
constructor:
  syntax: NodeDisruptionProfile()
variables:
- description: 'The policy configuration for when to allow certain operations

    which require node re-image and trigger redeployment. For example, some operations,
    such as

    updating the .properties.ManagedClusterSecurityProfile.customCATrustCertificates
    field on an

    existing managed cluster, trigger rolling updates of the nodes. This setting allows
    control

    over when such updates are accepted. The default is ''Allow''. For a full list
    of covered

    operations see aka.ms/aks/nodedisruptionpolicy". Known values are: "Allow",

    "AllowDuringMaintenanceWindow", and "Block".'
  name: node_disruption_policy
  types:
  - <xref:typing.Union>[<xref:str>, <xref:"NodeDisruptionPolicy">]
attributes:
- uid: azure.mgmt.containerservice.types.NodeDisruptionProfile.nodeDisruptionPolicy
  name: nodeDisruptionPolicy
  summary: 'The policy configuration for when to allow certain operations which require
    node re-image and

    trigger redeployment. For example, some operations, such as updating the

    .properties.ManagedClusterSecurityProfile.customCATrustCertificates field on an
    existing

    managed cluster, trigger rolling updates of the nodes. This setting allows control
    over when

    such updates are accepted. The default is ''Allow''. For a full list of covered
    operations see

    aka.ms/aks/nodedisruptionpolicy". Known values are: "Allow",

    "AllowDuringMaintenanceWindow", and "Block".'
  signature: 'nodeDisruptionPolicy: str | NodeDisruptionPolicy'
