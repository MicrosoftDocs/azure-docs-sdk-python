### YamlMime:PythonClass
uid: azure.mgmt.workloads.models.NetworkInterfaceResourceNames
name: NetworkInterfaceResourceNames
fullName: azure.mgmt.workloads.models.NetworkInterfaceResourceNames
module: azure.mgmt.workloads.models
inheritances:
- azure.mgmt.workloads._serialization.Model
summary: The resource names object for network interface and related resources.
constructor:
  syntax: 'NetworkInterfaceResourceNames(*, network_interface_name: str | None = None,
    **kwargs: Any)'
  parameters:
  - name: network_interface_name
    description: 'The full name for network interface. If name is not provided,

      service uses a default name based on the deployment type. For SingleServer,
      default name is

      {SID}-Nic. In case of HA-AvZone systems, default name will be {SID}-{App/ASCS/DB}-Zone{A/B}-Nic

      with an incrementor at the end in case of more than 1 instance per layer. For
      distributed and

      HA-AvSet systems, default name will be {SID}-{App/ASCS/DB}-Nic with an incrementor
      at the end

      in case of more than 1 instance per layer.'
    types:
    - <xref:str>
variables:
- description: 'The full name for network interface. If name is not provided,

    service uses a default name based on the deployment type. For SingleServer, default
    name is

    {SID}-Nic. In case of HA-AvZone systems, default name will be {SID}-{App/ASCS/DB}-Zone{A/B}-Nic

    with an incrementor at the end in case of more than 1 instance per layer. For
    distributed and

    HA-AvSet systems, default name will be {SID}-{App/ASCS/DB}-Nic with an incrementor
    at the end

    in case of more than 1 instance per layer.'
  name: network_interface_name
  types:
  - <xref:str>
