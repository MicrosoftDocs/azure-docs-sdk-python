### YamlMime:PythonModule
uid: azure.ai.agentserver.agentframework.models.agent_id_generator
name: agent_id_generator
fullName: azure.ai.agentserver.agentframework.models.agent_id_generator
summary: 'Helper utilities for constructing AgentId model instances.



  Centralizes logic for safely building a `models.AgentId` from a request agent

  object. We intentionally do not allow overriding the generated model''s fixed

  `type` literal ("agent_id"). If the provided object lacks a name, `None` is

  returned so callers can decide how to handle absence.'
classes:
- azure.ai.agentserver.agentframework.models.agent_id_generator.AgentIdGenerator
