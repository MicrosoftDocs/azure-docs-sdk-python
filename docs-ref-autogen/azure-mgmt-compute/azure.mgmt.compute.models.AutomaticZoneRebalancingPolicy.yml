### YamlMime:PythonClass
uid: azure.mgmt.compute.models.AutomaticZoneRebalancingPolicy
name: AutomaticZoneRebalancingPolicy
fullName: azure.mgmt.compute.models.AutomaticZoneRebalancingPolicy
module: azure.mgmt.compute.models
summary: The configuration parameters used while performing automatic AZ balancing.
constructor:
  syntax: 'AutomaticZoneRebalancingPolicy(*, enabled: bool | None = None, rebalance_strategy:
    str | _models.RebalanceStrategy | None = None, rebalance_behavior: str | _models.RebalanceBehavior
    | None = None, **kwargs: Any)'
  keywordOnlyParameters:
  - name: enabled
    description: 'Specifies whether Automatic AZ Balancing should be enabled on the
      virtual

      machine scale set. The default value is false.'
    defaultValue: None
    types:
    - <xref:bool>
  - name: rebalance_strategy
    description: 'Type of rebalance strategy that will be used for rebalancing

      virtual machines in the scale set across availability zones. Default and only
      supported value

      for now is Recreate. "Recreate"'
    defaultValue: None
    types:
    - <xref:str>
    - <xref:azure.mgmt.compute.models.RebalanceStrategy>
  - name: rebalance_behavior
    description: 'Type of rebalance behavior that will be used for recreating

      virtual machines in the scale set across availability zones. Default and only
      supported value

      for now is CreateBeforeDelete. "CreateBeforeDelete"'
    defaultValue: None
    types:
    - <xref:str>
    - <xref:azure.mgmt.compute.models.RebalanceBehavior>
variables:
- description: 'Specifies whether Automatic AZ Balancing should be enabled on the
    virtual

    machine scale set. The default value is false.'
  name: enabled
  types:
  - <xref:bool>
- description: 'Type of rebalance strategy that will be used for rebalancing virtual

    machines in the scale set across availability zones. Default and only supported
    value for now

    is Recreate. "Recreate"'
  name: rebalance_strategy
  types:
  - <xref:str>
  - <xref:azure.mgmt.compute.models.RebalanceStrategy>
- description: 'Type of rebalance behavior that will be used for recreating virtual

    machines in the scale set across availability zones. Default and only supported
    value for now

    is CreateBeforeDelete. "CreateBeforeDelete"'
  name: rebalance_behavior
  types:
  - <xref:str>
  - <xref:azure.mgmt.compute.models.RebalanceBehavior>
